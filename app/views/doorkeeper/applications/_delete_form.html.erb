<%- submit_btn_css ||= 'btn btn-danger btn-sm' %>
<%= form_tag oauth_application_path(application), method: :delete do %>
    <%= submit_tag t('doorkeeper.applications.buttons.destroy'), data: {confirm: t('doorkeeper.applications.confirmations.destroy')}, class: submit_btn_css %>
<% end %>