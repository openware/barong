<%= form_with(model: document, local: true) do |form| %>
  <% if document.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(document.errors.count, "error") %> prohibited this document from being saved:</h2>

      <ul>
      <% document.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <%= form.label :profile_id %>
    <%= form.text_field :profile_id, id: :document_profile_id, class: 'form-control underlined' %>
  </div>

  <div class="field">
    <%= form.label :doc_type %>
    <%= form.select(:doc_type, ['passport', 'identity card', 'driver license', 'utility bill'], {id: :document_doc_type}, {class: 'form-control underlined'}) %>
  </div>

  <div class="field form-group">
    <%= form.label :doc_number %>
    <%= form.text_field :doc_number, id: :document_doc_number, class: 'form-control underlined' %>
  </div>

  <div class="field form-group">
    <div class="col-6">
      <%= form.text_field :dob, id: :profile_dob, class: 'datepicker', placeholder: 'Expiry date' %>
    </div>
  </div>

  <div class="actions">
    <%= form.submit 'Submit', class: 'btn btn-sm btn-success' %>
  </div>
  <hr>
<% end %>
